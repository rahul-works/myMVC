<?php

// login 

// get user -> <email->email, (e->email, p->password), company-key->company_id> admin, employee, customer, data
   // admin + data
   // [admin + data, employee + data]
   // employee + data
   // customer + data 


   // job 

   
// get job -> <company-key->company_id, (job-key->job-id, company-key->company_id)> data
   // data
